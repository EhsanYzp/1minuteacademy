{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://1minuteacademy.dev/schemas/story.schema.json",
  "title": "1MinuteAcademy Story",
  "description": "A 6-beat narrative structure for micro-learning modules. Each beat displays for 8 seconds. Generation targets 120 chars (punchline: 80). Validation hard-caps at 130 (punchline: 90) to allow a small tolerance buffer.",
  "type": "object",
  "required": ["hook", "buildup", "discovery", "twist", "climax", "punchline"],
  "properties": {
    "hook": { "$ref": "#/$defs/beat" },
    "buildup": { "$ref": "#/$defs/beat" },
    "discovery": { "$ref": "#/$defs/beat" },
    "twist": { "$ref": "#/$defs/beat" },
    "climax": { "$ref": "#/$defs/beat" },
    "punchline": { "$ref": "#/$defs/punchlineBeat" }
  },
  "$defs": {
    "beat": {
      "type": "object",
      "required": ["text", "visual"],
      "properties": {
        "text": { "type": "string", "minLength": 1, "maxLength": 130 },
        "visual": { "type": "string", "minLength": 1 }
      }
    },
    "punchlineBeat": {
      "type": "object",
      "required": ["text", "visual"],
      "properties": {
        "text": { "type": "string", "minLength": 1, "maxLength": 90 },
        "visual": { "type": "string", "minLength": 1 }
      }
    }
  }
}
